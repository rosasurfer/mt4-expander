#pragma once

#define  NOMINMAX                      // alternative work-around: std::min<int>(3, 4)
#define  WIN32_LEAN_AND_MEAN           // exclude rarely-used stuff from Windows headers
#include <windows.h>
